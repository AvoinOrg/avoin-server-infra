# ─────────────────────────────────────────────────────────────
# Basics
# ─────────────────────────────────────────────────────────────
# Where to store persistent data on your host
DATA_PATH=/srv/directus-data

# The public domain for Directus (Traefik host rule)
DIRECTUS_DOMAIN=directus.example.com
PUBLIC_URL=https://directus.example.com

# Traefik cert resolver name (matches your Traefik config)
TRAEFIK_CERTRESOLVER=myresolver

# Pin a Directus image or leave blank for latest
DIRECTUS_IMAGE_TAG=latest

# ─────────────────────────────────────────────────────────────
# Database (Postgres)
# ─────────────────────────────────────────────────────────────
DIRECTUS_DB_NAME=directus
DIRECTUS_DB_USER=directus
DIRECTUS_DB_PASSWORD=supersecretdbpassword

# ─────────────────────────────────────────────────────────────
# Directus bootstrap + security
# ─────────────────────────────────────────────────────────────
# Long random strings
DIRECTUS_KEY=change-this-long-random-string
DIRECTUS_SECRET=change-this-very-long-random-string

# First admin (only used on first boot if DB is empty)
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=change-this-admin-password

# ─────────────────────────────────────────────────────────────
# CORS (optional—only if your Next.js calls Directus from another origin)
# ─────────────────────────────────────────────────────────────
CORS_ENABLED=false
# Example: CORS_ORIGIN=https://app.example.com,https://admin.example.com
CORS_ORIGIN=

# ─────────────────────────────────────────────────────────────
# ZITADEL OIDC (SSO)
# ─────────────────────────────────────────────────────────────
# Create an OIDC app in Zitadel and paste values here
AUTH_ZITADEL_CLIENT_ID=
AUTH_ZITADEL_CLIENT_SECRET=
# Issuer .well-known URL, eg:
# https://<your-zitadel-domain>/.well-known/openid-configuration
AUTH_ZITADEL_ISSUER_URL=

# Which claim holds user groups/roles? Common choices:
# - groups   (if you add a small Zitadel Action to emit roles as "groups")
# - urn:zitadel:iam:org:project:roles   (default style)
AUTH_ZITADEL_GROUP_CLAIM_NAME=groups

# Optional: map groups → Directus role UUIDs
# Example (keep the json: prefix):
# AUTH_ZITADEL_ROLE_MAPPING=json:{"admin":"<uuid-admin>","editor":"<uuid-editor>"}
AUTH_ZITADEL_ROLE_MAPPING=
